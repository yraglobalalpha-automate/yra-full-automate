name: Auto-fill Missing Categories (one-off)

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Report choices only (yes/no)"
        default: "yes"

jobs:
  autocat:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Auto-fill categories
        env:
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
          DRY_RUN: ${{ inputs.dry_run == 'no' && '0' || '1' }}
        run: python autofill_categories.py
